server:
  port: 8080

spring:
  application:
    name: demo-flynow-api
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jmx:
    enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/demoflynowdb?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: demoflynow
